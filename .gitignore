# OS
Thumbs.db
desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Node (สำหรับตอนเพิ่ม backend/build step ในอนาคต)
node_modules/
dist/
build/
*.log
.env
.env.*
!.env.example

# ข้อมูลดิบ bathymetry / shapefile ที่ไม่ควร commit
data/raw/
*.shp
*.shx
*.dbf
*.prj
*.gpkg
*.tif

# กริดความลึกดิบที่ดึงมาสร้างเส้น — 8 MB ไม่ต้องเก็บใน repo
# สร้างใหม่ได้ด้วย: node scripts/build-bathymetry.mjs --refresh
scripts/bathy-south.csv

# ตารางดิบของ FishBase และกรมประมงที่ scripts/build-species.py ดึงมา — รวมกันเกิน 150 MB
# ดึงใหม่ได้ด้วย: python scripts/build-species.py --refresh
scripts/.cache/
