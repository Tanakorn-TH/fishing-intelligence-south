# แนวทางร่วมพัฒนา

ขอบคุณที่มาช่วยกันครับ เอกสารนี้บอกวิธีเริ่มงาน กติกาการส่งโค้ด และสิ่งที่ CI จะตรวจ

## เริ่มต้น

```bash
git clone https://github.com/Tanakorn-TH/fishing-intelligence-south.git
```

โปรเจคเป็น HTML + CSS + JavaScript ล้วน **ไม่มี build step ไม่มี dependency** เปิด `index.html` ตรง ๆ ได้เลย
หรือรันผ่าน local server (จำเป็นถ้าจะทดสอบพฤติกรรมที่ต้องใช้ HTTP):

```bash
python -m http.server 5173
```

## โครงสร้างไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | โครงหน้าเว็บทั้งหมด รวม dialog ปฏิทินทริป |
| `styles.css` | สไตล์ทั้งหมด |
| `app.js` | interaction ทั้งหมด — nav, planner, ปฏิทิน, toast |
| `data-model.sql` | สคีมา MySQL 8 |
| `api/` | backend PHP — `spots.php`, `gear.php`, `health.php` และ `lib/` |
| `tests/` | ชุดทดสอบ API ที่ CI รัน |
| `BATHYMETRY_IMPORT.md` | ขั้นตอนนำเข้าชั้นข้อมูลความลึก |
| `scripts/check-dom-ids.mjs` | ตัวตรวจของ CI |
| `scripts/snapshot-colors.mjs` | เก็บสีที่คำนวณได้จริงไว้เทียบก่อน-หลังตอนแก้ CSS (ไม่ได้อยู่ใน CI) |
| `.htmlvalidate.json` | ชุดกฎตรวจ HTML |

## สภาพแวดล้อมปลายทาง

ตรวจไว้เมื่อ 8 ส.ค. 2569 จากหน้า phpMyAdmin ของ `fishing.yru.ac.th`

| | |
|---|---|
| Web server | Apache/2.4.65 (Ubuntu) + mod_fcgid อยู่หลัง nginx |
| PHP | **8.4.11** |
| MySQL | 8.0.46 (Ubuntu) ต่อผ่าน UNIX socket |
| charset | utf8mb4 |

เขียนโค้ดให้รันได้ตั้งแต่ **PHP 8.1** เผื่อเซิร์ฟเวอร์ถูกลดเวอร์ชัน — CI ตรวจไวยากรณ์ทั้ง 8.1 และ 8.4
ส่วน integration test รันบน 8.4 ให้ตรงกับของจริง

## วิธีทำงาน

1. แตก branch จาก `main` ตั้งชื่อสื่อความหมาย เช่น `feat/tide-api`, `fix/calendar-offset`
2. commit เป็นก้อนย่อย ๆ ที่อธิบายได้ว่าแก้อะไรและ**ทำไม**
3. เปิด Pull Request เข้า `main` — push ตรงเข้า `main` ไม่ได้ ต้องผ่าน PR และ CI ต้องเขียว
4. ให้เพื่อนร่วมทีมรีวิวก่อน merge — ตอนนี้ ruleset ตั้ง required approvals ไว้ที่ `0`
   เพราะยังทำกันคนเดียว (GitHub ไม่ให้ approve PR ของตัวเอง) พอมีคนที่สองแล้วให้ปรับกลับเป็น `1`

## CI ตรวจอะไรบ้าง

ทุก PR จะรัน [.github/workflows/ci.yml](.github/workflows/ci.yml) 4 job

> ⚠️ **ห้ามเปลี่ยนค่า `name:` ของ job ใน `ci.yml` โดยไม่แก้ ruleset ตาม**
> branch protection บังคับ check ตาม**ชื่อ** ถ้าเปลี่ยนชื่อ job แล้วไม่แก้ ruleset
> PR ทุกอันจะค้างรอ check ที่ไม่มีวันมา ทั้งที่ CI เขียวหมด
> แก้ที่ Settings → Rules → protect-main → Require status checks to pass
> (เคยเกิดมาแล้วตอนเปลี่ยนสคีมาจาก PostgreSQL เป็น MySQL)

**`frontend` — ตรวจฝั่งหน้าเว็บ**

| ขั้นตอน | รันเองในเครื่อง |
|---|---|
| ไวยากรณ์ JavaScript | `node --check app.js` |
| id ที่ `app.js` เรียกหา มีจริงใน `index.html` | `node scripts/check-dom-ids.mjs` |
| HTML ถูกตามสเปกและ accessibility | `npx html-validate index.html` |
| media query ปลอดภัยกับ Safari เก่า | `node scripts/check-media-queries.mjs` |

> ⚠️ **สคริปต์ใน `scripts/` ต้องรันได้บน Node 20** ซึ่งเป็นเวอร์ชันที่ CI ใช้
> เครื่องนักพัฒนามักใหม่กว่า ชุดตรวจในเครื่องจึงผ่านทั้งที่ CI พัง
> เคยพลาดมาแล้วกับ `fs.globSync` ที่เพิ่งมีใน Node 22 — สคริปต์ตายตั้งแต่นำเข้าโมดูล
> ทดสอบด้วยเวอร์ชันเดียวกับ CI ได้ด้วย `npx -y node@20 scripts/ชื่อไฟล์.mjs`

ตัวตรวจ id มีไว้เพราะบั๊กแบบนี้เคยเกิดจริง — `getElementById` คืน `null` แล้วสคริปต์ตายเงียบตอนโหลดหน้า
หน้าเว็บดูเหมือนปกติแต่กดอะไรไม่ได้เลย และจะไม่เห็นอะไรถ้าไม่เปิด devtools

**`schema` — ตรวจสคีมาฐานข้อมูล**

รัน `data-model.sql` บนฐานข้อมูลเปล่า เช็คว่า view เรียกได้ seed ครบ และ**ลำดับแกน SRID 4326 ยังเป็น Lat ก่อน Lon**
job นี้มีเพราะเคยมีบั๊กที่ view เลือกคอลัมน์ที่ยังไม่มีในตาราง ทำให้ไฟล์รันไม่ผ่านตั้งแต่ต้น
ส่วนขั้นตรวจลำดับแกนมีไว้กันกรณีที่ร้ายกว่า — ถ้าแกนสลับ พิกัดทั้งระบบจะผิดโดยไม่มีใครรู้

รันเองในเครื่อง (ต้องมี Docker):

```bash
docker run --rm -d --name fishing-db -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=fishing -p 3306:3306 mysql:8.0
```

```bash
MYSQL_PWD=root mysql -h 127.0.0.1 -uroot --default-character-set=utf8mb4 fishing < data-model.sql
```

**`api` — ตรวจ API**

ตรวจไวยากรณ์ PHP บน 8.1 และ 8.4 แล้วยกเซิร์ฟเวอร์ PHP ขึ้นมาจริง ยิง HTTP เข้า endpoint
ทดสอบกับข้อมูลใน `tests/fixtures.sql` — รวมถึงข้อที่ยืนยันว่า API อ่านละติจูด/ลองจิจูดไม่สลับด้าน
การรับค่าที่ไม่ถูกต้อง และการที่ `health.php` ไม่รั่วค่าเชื่อมต่อ

รันเองในเครื่อง (ต้องมี PHP + ฐานข้อมูลจาก job ก่อนหน้า):

```bash
php -S 127.0.0.1:8080 -t .
```

```bash
DB_HOST=127.0.0.1 DB_NAME=fishing DB_USER=root DB_PASSWORD=root API_BASE=http://127.0.0.1:8080 php tests/api-test.php
```

job นี้ยังวัด**พื้นที่กด**ด้วย เพราะต้องใช้เซิร์ฟเวอร์ที่ API ทำงานจริง
ถ้าวัดบน static server ส่วนที่รอข้อมูลจะไม่ถูกวาด แล้วจะสรุปว่า "ผ่านแล้ว" ทั้งที่ยังไม่เห็นของจริง
— เคยพลาดแบบนี้มาสองรอบ

```bash
npm install --no-save playwright@1 && npx playwright install chromium
```

```bash
TAP_BASE=http://127.0.0.1:8080 node scripts/check-tap-targets.mjs
```

วัดที่ 390 / 820 / 1280 โดยเปิดโหมดสัมผัสทุกช่วง เพราะเกณฑ์ผูกกับ `pointer: coarse`
ไม่ใช่ความกว้างจอ — แท็บเล็ตและจอสัมผัสขนาดใหญ่ก็ใช้นิ้วเหมือนกัน

## เลขเวอร์ชัน

ใช้ [Semantic Versioning](https://semver.org/) ผูกกับ git tag เวอร์ชันปัจจุบันแสดงที่ท้ายหน้าเว็บ

**แหล่งความจริงมีที่เดียว** — ค่า `APP_VERSION` บนหัวไฟล์ [app.js](app.js)
ส่วนค่าใน `index.html` เป็นแค่ตัวสำรองตอน JS ยังไม่ทำงาน ต้องแก้ให้ตรงกันเสมอ

ปล่อยรุ่นใหม่:

```bash
git tag -a v0.3.2 -m "สรุปสั้น ๆ ว่ารุ่นนี้มีอะไร" && git push origin v0.3.2
```

เกณฑ์การขยับเลข — โปรเจคยังอยู่ `0.x` API จึงถือว่ายังไม่นิ่ง

| ส่วน | ขยับเมื่อ |
|---|---|
| `0.X.0` | เพิ่มความสามารถใหม่ หรือเปลี่ยนโครงสร้างข้อมูล/สคีมา |
| `0.0.X` | แก้บั๊ก ปรับดีไซน์ ปรับเอกสาร |

## แนวทางเขียนโค้ด

- **ไม่เพิ่ม dependency หรือ build step** โดยไม่คุยกันก่อน จุดขายของโปรเจคคือเปิดไฟล์แล้วรันได้เลย
- **ข้อความ UI เป็นภาษาไทยทั้งหมด** ส่วนชื่อตัวแปร ชื่อฟังก์ชัน และ commit message เป็นภาษาอังกฤษ
- **สีใช้ CSS custom properties** ที่ประกาศไว้ใน `:root` เท่านั้น อย่าฮาร์ดโค้ดสีใหม่
  `styles.css` ไม่มีสีดิบเหลือแล้ว (v0.9.8) ถ้าต้องใช้เฉดใหม่ให้ประกาศเป็นตัวแปรก่อน
  ตอนแก้ CSS ที่ห้ามให้หน้าตาเปลี่ยน ใช้ `scripts/snapshot-colors.mjs` เทียบก่อน-หลัง
- **วันที่ต้องคำนวณจาก `Date` เสมอ** อย่าฮาร์ดโค้ดชื่อวันหรือชื่อเดือน — เคยพลาดมาแล้ว
- **แยก `.today` กับ `.selected` ให้ชัด** "วันนี้" กับ "วันที่ผู้ใช้เลือก" เป็นคนละสถานะ
- **ค่าที่เป็นข้อมูลตัวอย่าง ให้เขียนคอมเมนต์กำกับ** ว่ารอต่อ API อะไร
- **ห้ามเขียน media query แบบ range syntax** เช่น `@media (width < 40rem)` — Safari ก่อน 16.4
  ทิ้งทั้งบล็อกเงียบ ๆ ไม่มี error ให้เห็น เขียนซ้อนสองชั้นแทน มีตัวดักใน CI แล้ว
- **ระยะกันติ่งจอใช้โทเคน `--sa-t/-r/-b/-l`** ไม่เรียก `env()` ตรง ๆ เพราะต้องหารตัวคูณซูมก่อน
  ของที่เป็น `position: fixed` ไม่ได้ padding จาก body ต้องกันเองหรือใส่คลาส `.safe-x`
- **พื้นที่กดวัดที่ `pointer: coarse` ไม่ใช่ความกว้างจอ** แท็บเล็ตจอใหญ่ก็ใช้นิ้ว
  ขยายด้วย `::after` ที่วางทับ ไม่ใช่เพิ่มขนาดกล่องจริง จะได้ไม่ทำแถวเสียสัดส่วน
- **พิกัดใน MySQL เขียน `POINT(lat lon)` เสมอ** — SRID 4326 บน MySQL ใช้ละติจูดก่อน ตรงข้ามกับ PostGIS
  ถ้าคุณคุ้นกับ PostGIS หรือ GeoJSON (ซึ่งใช้ lon ก่อน) ต้องสลับก่อนเขียนลงฐานข้อมูล
- **คิวรีระยะทางต้องกรองด้วย `MBRIntersects` ก่อนเสมอ** MySQL ใช้ SPATIAL INDEX ได้เฉพาะฟังก์ชันตระกูล MBR
  ถ้าเรียก `ST_Distance_Sphere` ตรง ๆ อย่างเดียว จะสแกนทั้งตาราง — ดูตัวอย่างท้าย `data-model.sql`

## หนี้ทางเทคนิคที่รู้อยู่แล้ว

ไม่ต้องแจ้งซ้ำ แต่ถ้าใครอยากเก็บก็ยินดีครับ

- **ตัวเลขในหน้าเว็บเกือบทั้งหมดเป็นข้อมูลตัวอย่าง** — คะแนน กราฟน้ำ พยากรณ์รายชั่วโมง Solunar สภาพอากาศ ยังฮาร์ดโค้ดอยู่ใน `index.html` และ `app.js`
- **ปุ่มเลื่อนเดือนในปฏิทินถูก `disabled` ไว้** เพราะยังไม่มี API คะแนนของเดือนอื่น
- **`styles.css` ถูก minify เป็นบรรทัดเดียว** ทำให้ diff อ่านยากและ merge ชนกันง่ายมาก ถ้าทีมโตขึ้นควรคุยกันว่าจะจัดรูปแบบใหม่ทั้งไฟล์ในครั้งเดียว (ทำเป็น PR แยกที่ไม่แก้อย่างอื่นเลย)
- **ปุ่มส่วนใหญ่ไม่มี `type="button"`** ตอนนี้ไม่มีปัญหาเพราะยังไม่มี `<form>` แต่ต้องใส่ให้ครบก่อนเพิ่มฟอร์มบันทึกปลา ไม่งั้นปุ่มจะ submit ฟอร์มโดยไม่ตั้งใจ
- **ความสูงของแท่งกราฟรายชั่วโมงใช้ inline style** เพราะเป็นค่าที่มาจากข้อมูล ตั้งใจไว้แบบนี้

## กติกาเรื่องข้อมูล

อ่าน [BATHYMETRY_IMPORT.md](BATHYMETRY_IMPORT.md) ก่อนแตะอะไรที่เกี่ยวกับความลึกท้องทะเล สรุปสั้น ๆ

- **ข้อมูลความลึกในโปรเจคนี้ใช้เพื่อวางแผนตกปลาเท่านั้น ห้ามใช้เพื่อการเดินเรือ** ข้อความเตือนที่มีอยู่ในหน้าเว็บและใน SQL ห้ามลบ
- ทุกชุดข้อมูลที่นำเข้า ต้องบันทึก URL ต้นทาง ผู้เผยแพร่ สัญญาอนุญาต และวันที่ดึงข้อมูลลงตาราง `data_sources`
- ค่าความลึกต้องมาพร้อม vertical datum เสมอ อย่าเดาหรือเว้นว่าง
- ห้ามอนุมานความลึกใกล้ฝั่งจากกริดข้อมูลระดับโลก
